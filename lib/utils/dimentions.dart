import 'package:get/get.dart';

class Dimentions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;
  //main_food_page
  static double containerHeight = screenHeight / 2.5;
  static double imageContainerHeight = screenHeight / 3.5;
  static double detailContainerHeight = 110;

  //food_details_page
  static double foodContainerHeight = screenHeight / 2.4;
}
